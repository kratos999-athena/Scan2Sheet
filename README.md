# Scan2Sheet
This project is about converting the scanned images with tables into a excel sheet 
<br><br>
It uses opencv to preprocess the image as well as extract the table structure from it and then utilizes paddleocr,edsr and tesseract for the conversion of each cell into text , the text is then carefully placed into its correct position in the csv